# Subscriber side of a cross-scope topic.
# Declares matching type (consistency rule allows this).
version: 1

nodes:
  consumer:
    sub:
      in:
        min_rate_hz: 10

topics:
  shared_data:
    type: std_msgs/msg/String
    sub: [consumer/in]
