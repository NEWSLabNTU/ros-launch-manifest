# Subscriber side with WRONG type (consistency rule should error).
version: 1

nodes:
  bad_consumer:
    sub:
      in:
        min_rate_hz: 10

topics:
  shared_data:
    type: std_msgs/msg/Int32
    sub: [bad_consumer/in]
